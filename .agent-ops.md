# 🤖 Agent Operations File
> Bu dosya GitHub Copilot'un her işlem sonrası güncellediği kural + değişiklik kaydıdır.
> Bir sonraki işlemde önce bu dosyayı oku, kurallara uy.

---

## ⚙️ Zorunlu Kurallar

1. **Her kod değişikliğinden sonra versiyonu güncelle** (`src/app/lib/siteConfig.js` → `version`)
   - Küçük UI/UX değişikliği → patch artır (1.7.6 → 1.7.7)
   - Yeni feature → minor artır (1.7.x → 1.8.0)
   - Breaking change → major artır (1.x → 2.0.0)
2. **Her değişikliği Changelog'a ekle** (bu dosyanın alt kısmı)
3. **Her işlemden sonra `get_errors()` çağır**, hata varsa düzelt
4. **Yeni component/lib dosyası oluştururken** `authorConfig`, `siteConfig`, `cityConfig` pattern'ını takip et (tek sorumluluk, export named)
5. **Tailwind** — `z-[N]` kullanma, `z-N` kullan; `min-w-[Npx]` yerine Tailwind arbitrary değeri veya scale
6. **Server/Client** — `"use client"` sadece gerçekten client state/event gerektiğinde
7. **Yeni API route** — her zaman `try/catch` + anlamlı HTTP status kodu döndür
8. **localStorage** — her zaman SSR-safe yaz (`typeof window !== "undefined"`)

---

## 📦 Mevcut Versiyon

**v1.8.5**

---

## 🗺️ Aktif Özellik Durumu

| Özellik                            | Durum   | Dosya                              |
| ---------------------------------- | ------- | ---------------------------------- |
| Haber akışı (RSS + NewsData)       | ✅ Aktif | `lib/newsSource.js`                |
| Kategori sayfaları + swipe         | ✅ Aktif | `category/[slug]/page.jsx`         |
| AI Günlük Özet                     | ✅ Aktif | `lib/dailySummary.js`              |
| Push bildirim (VAPID)              | ✅ Aktif | `api/cron/push-notify/`            |
| Kaynak güvenilirlik skoru          | ✅ Aktif | `lib/sourceCredibility.js`         |
| Makale analizi + karşılaştırma     | ✅ Aktif | `lib/analyzeArticle.js`            |
| Piyasa / Hava verisi (Redis cache) | ✅ Aktif | `lib/realTimeData.js`              |
| Şehir seçici                       | ✅ Aktif | `components/CityWeatherWidget.jsx` |
| Admin panel                        | ✅ Aktif | `components/AdminDashboard.jsx`    |
| Pull to refresh                    | ✅ Aktif | `hooks/usePullToRefresh.js`        |
| DailySummary mobil collapse        | ✅ Aktif | `components/DailySummary.jsx`      |
| About sayfası                      | ✅ Aktif | `app/about/page.jsx`               |
| **Zaman filtresi**                 | ✅ Aktif | `components/NewsFeed.jsx`          |
| **Okuma istatistikleri**           | ✅ Aktif | `lib/useReadingStats.js`           |

---

## 🚦 Kısa Vadeli Roadmap

- [ ] In-app reader mode (makaleyi dış siteye açmadan okuma)
- [ ] Kategoriye özel push bildirim tercihi
- [ ] E-posta özet aboneliği
- [ ] Kaynak takibi ("Cumhuriyet haberlerini takip et")
- [ ] Arama geçmişi
- [ ] Bildirim batch / paralel gönderim (>500 kullanıcı için)

---

## 📝 Changelog

### v1.8.5 — 2026-03-04
- 📦 NewsFeed `showTabs` prop eklendi (default `true`); kategori sayfalarında `showTabs={false}` geçiriliyor — tab bar + swipe sadece ana sayfada görünüyor

### v1.8.4 — 2026-03-04
- ✨ **Haber detay paylaş butonu**: `ShareButton.jsx` oluşturuldu (Web Share API + clipboard fallback); makale başlığının altında "Paylaş" + "Kaydet" aksiyonları eklendi
- ✨ **BookmarkButton `showLabel` prop**: detay sayfasında etiketli ("Kaydet / Kaydedildi") görünüyor
- 🔤 **Detay sayfası kategoriler Türkçe**: `CATEGORY_LABELS` map ile badge'ler artık Türkçe
- 🐞 **"Senin İçin" boş kalıyordu**: `articleMatchesPrefs` kategori karşılaştırması büyük/küçük harf bağımsız yapıldı
- 🐞 **Ayarlardan sonra "Senin İçin" güncellenmiyordu**: NewsFeed'e `storage` olay dinleyicisi eklendi; `preferredCategories` ayarlardan değişince feed anında güncelleniyor

### v1.8.3 — 2026-03-04
- 📱 **Twitter tarzı sekmeler**: NewsFeed artık "Senin İçin" + "Gündem" sekmelerine bölündü; sola/sağa kaydırarak veya sekmeye dokunarak geçiş yapılıyor
- 🔤 **Kategori isimleri Türkçe**: Tüm NewfCard kategori chip'leri `CATEGORY_LABELS` map ile görüntüleniyor
- 🗂️ `CATEGORY_LABELS` export'u `categoryConfig.js`'e eklendi
- 🗂️ `categoryLabel()` helper NewsCard'a eklendi
- 🗂️ `TimeFilterBar` ayrı yardımcı bileşen olarak çıkarıldı (her iki panel de kullanıyor)
- `SectionDivider` ve eski düzen kodları dosyada sessizce bırakıldı (unused, zararsız)

### v1.8.2 — 2026-03-04
- 📱 **Mobil UX iyileştirmeleri**: zaman filtresi yan kaydırmalı satır, "Senin İçin" başlığı amber aksanlı badge/sayı
- ✨ **NewsCard `featured` prop**: mobilde dikey full-görsel layout; ilk 3 "Senin İçin" kartı mobilde büyük görsel gösterir
- 🖥️ **"Diğer Haberler" desktop**: 2-3 sütunlu grid, mobilde tek sütun doğal akış
- 🖥️ **Load more** mobilde tam genişlik (`w-full md:w-auto`)
- 🐞 Boş durum: amber çerçeve + düzenle linki ile iyileştirildi

### v1.8.1 — 2026-03-04
- ✨ **Kişiselleştirilmiş feed**: NewsFeed artık tercih varsa "Senin İçin" + "Diğer Haberler" olmak üzere iki bölüme ayrılıyor
- 🗂️ `prefs` state eklendi (followedTopics + preferredCategories), render'da kullanılıyor
- 🗂️ `forYouArticles` / `otherArticles` useMemo split eklendi
- 🗂️ `SectionDivider` yardımcı bileşeni eklendi (NewsFeed.jsx içi)
- 🗂️ `articleMatchesPrefs()` helper eklendi

### v1.8.0 — 2026-03-04
- ✨ **Onboarding akışı**: 5 adımlı animasyonlu ilk kurulum ekranı (karşılama → kategoriler → konular → şehir → bildirim)
- ✨ **Konu takibi**: `followedTopics` tercihi, NewsFeed'de eşleşen haberler üste sıralanır
- 🗂️ `OnboardingFlow.jsx` + `OnboardingProvider.jsx` oluşturuldu
- 🗂️ `DEFAULT_PREFERENCES.followedTopics` + `sortByFollowedTopics()` eklendi
- 🗂️ `layout.js` `OnboardingProvider` ile güncellendi

### v1.7.7 — 2026-03-03
- ✨ **Zaman filtresi**: NewsFeed'e "Son 1 saat / Bugün / Bu hafta / Tümü" pill filtreleri eklendi
- ✨ **Okuma istatistikleri**: `useReadingStats` hook + `ReadingStats` component oluşturuldu
- 🗂️ `ReadingStats` settings sayfasına "📊 Okuma İstatistiklerin" bölümü olarak eklendi
- 🤖 `.agent-ops.md` oluşturuldu (bu dosya)

### v1.7.6 — 2026-03-03
- ✨ About sayfası (`/about`) + footer linki eklendi
- 🗂️ `authorConfig.js` oluşturuldu — yazar/proje bilgileri tek dosyada

### v1.7.5 — 2026-03-03
- 🐛 City dropdown boyutu küçültüldü, relative pozisyon düzeltildi
- ✨ DailySummary mobil collapse (expand toggle)
- ✨ Pull to refresh (PullToRefresh.jsx)

### v1.7.4 — 2026-03-03
- ✨ Admin panel: "Piyasaları Güncelle" + "Hava Durumunu Güncelle" butonları
- ✨ `/api/markets` endpoint oluşturuldu
- 🗂️ `cityConfig.js` oluşturuldu — 10 şehir tek dosyada
- ⚡ Redis TTL artırıldı: piyasa 1sa, hava 2sa

### v1.7.3 — önceki oturum
- 🐛 `summary/loading.jsx` duplicate içerik temizlendi
- 🐛 setState-in-effect hatası düzeltildi (Navigation, CityWeatherWidget)
- 🐛 Tailwind `z-[N]` kısaltmaları düzeltildi (nav, toolbar, search, offline)
- 🐛 `<img>` → `<Image />` (3 yer, summary/page.jsx)
- 🐛 `exampleSummaryData.js` double-wrapped weather düzeltildi
